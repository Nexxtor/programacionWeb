/** This example was designed to run in a web browser's JavaScript console,
 * but it can also be run with Node.js as long as it doesn't include DOM examples. */

// for statements

(function () {
  'use strict';

  for (let i = 0; i <= 10; i++) {
    console.log(i);
  }

  // The are other syntax, see object and array examples
})();
